# MISFILS
Run python -m pip install requirements.txt to install the prequesite files for this project. 

Run xlsx_to_mongo.py to transfer the XLSX file to your mongo database along with created indexes.

After, to get a basic view of recent 10 records from your MongoDB, you can run python app.py and navigate to "http://127.0.0.1:5000/records" 

